<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.aschyiel.rpg"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.Launcher"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> 
        </activity>
        <activity android:name=".activities.Terrain" android:configChanges="orientation" />
        <activity android:name=".activities.Sandbox" android:configChanges="orientation" />
        <activity android:name=".activities.sandbox.BasicMovement"       android:configChanges="orientation" />
        <activity android:name=".activities.sandbox.MovementVsLandTypes" android:configChanges="orientation" />
        <activity android:name=".activities.sandbox.BasicCombat"         android:configChanges="orientation" />
    </application>

</manifest>
